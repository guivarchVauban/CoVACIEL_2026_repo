services:
  ros2_tuto_robot:
    build: .
    container_name: docker_cmdvel
    restart: always
    network_mode: host
    privileged: true
    volumes:
      - "./src:/root/ros_ws/src"
    environment:
      - RMW_IMPLEMENTATION=rmw_cyclonedds_cpp
      - ROS_DOMAIN_ID=67
    tty: true
